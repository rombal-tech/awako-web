import React from 'react';
import { Typography } from '@material-ui/core';

const SchematicsCount = () => (
	<Typography className="schematics-count" variant="subtitle1">
		Schematics found:
	</Typography>
);

export default SchematicsCount;
